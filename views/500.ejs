<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Internal Server Error | VulnNode CTF</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            min-height: 100vh;
            padding: 40px 20px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .error-container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        }
        .error-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .error-code {
            font-size: 80px;
            font-weight: bold;
            color: #f5576c;
            margin: 0;
        }
        .error-title {
            font-size: 28px;
            color: #333;
            margin: 10px 0;
        }
        .error-message {
            color: #666;
            font-size: 16px;
        }
        .stack-trace {
            background: #2d2d2d;
            color: #00ff00;
            padding: 20px;
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-size: 13px;
            overflow-x: auto;
            margin: 20px 0;
            max-height: 400px;
            overflow-y: auto;
        }
        .stack-trace::-webkit-scrollbar {
            width: 8px;
        }
        .stack-trace::-webkit-scrollbar-track {
            background: #1a1a1a;
        }
        .stack-trace::-webkit-scrollbar-thumb {
            background: #00ff00;
            border-radius: 4px;
        }
        .error-details {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .btn-action {
            background: #f5576c;
            color: white;
            padding: 10px 25px;
            border-radius: 20px;
            text-decoration: none;
            display: inline-block;
            margin: 10px 5px;
            transition: all 0.3s;
        }
        .btn-action:hover {
            background: #d63f57;
            transform: translateY(-2px);
            color: white;
        }
        .warning-badge {
            background: #ff9800;
            color: white;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 12px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-header">
            <h1 class="error-code">🚫 500</h1>
            <h2 class="error-title">Internal Server Error</h2>
            <p class="error-message">Something went wrong on our end. Don't worry, it's not your fault!</p>
        </div>

        <% if (typeof error !== 'undefined' && error && error.message) { %>
        <div class="error-details">
            <h4>🐛 Error Details:</h4>
            <p><strong>Message:</strong> <%= error.message %></p>
            <% if (error.code) { %>
                <p><strong>Error Code:</strong> <%= error.code %></p>
            <% } %>
            <% if (error.sql) { %>
                <p><strong>SQL Query:</strong> <code><%= error.sql %></code></p>
            <% } %>
        </div>

        <!-- VULNERABILITY: Stack Trace Exposure (Information Disclosure) -->
        <div class="alert alert-warning" role="alert">
            <span class="warning-badge">VULNERABILITY</span>
            This is intentionally exposing sensitive information for educational purposes.
        </div>

        <% if (error.stack) { %>
        <h5>Stack Trace:</h5>
        <div class="stack-trace">
<%= error.stack %>
        </div>
        <% } %>
        <% } %>

        <!-- Debug Information (Always shown in CTF environment) -->
        <div class="error-details">
            <h5>🛠️ Debug Information:</h5>
            <p><strong>Node Version:</strong> <%= process.version %></p>
            <p><strong>Platform:</strong> <%= process.platform %></p>
            <p><strong>Memory Usage:</strong> <%= Math.round(process.memoryUsage().heapUsed / 1024 / 1024) %> MB</p>
            <p><strong>Uptime:</strong> <%= Math.round(process.uptime()) %> seconds</p>
            <p><strong>Timestamp:</strong> <%= new Date().toISOString() %></p>
            <% if (typeof user !== 'undefined' && user) { %>
                <p><strong>User:</strong> <%= user.username %> (ID: <%= user.id %>)</p>
            <% } %>
        </div>

        <!-- CTF Flag in HTML comment -->
        <!-- FLAG{information_disclosure_via_error_pages} -->
        <!-- Congratulations! You found a flag by exploring error pages -->

        <div class="text-center">
            <a href="/" class="btn-action">🏠 Back to Home</a>
            <a href="/scoreboard" class="btn-action">🏆 View Scoreboard</a>
            <a href="javascript:history.back()" class="btn-action">← Go Back</a>
        </div>

        <div class="mt-4 text-center text-muted">
            <small>
                🚫 <strong>Warning:</strong> This is an intentionally vulnerable application for educational purposes.
                These error messages are designed to leak information.
            </small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>