

<%- include('layout', { body: '' }) %>

<div class="container mt-4">
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <img src="https://via.placeholder.com/150" class="card-img-top p-3" alt="Profile Pic">
                <div class="card-body text-center">
                    <h5 class="card-title"><%= profileUser.username %></h5>
                    <% if (profileUser.isAdmin) { %>
                        <span class="badge badge-danger">Administrator</span>
                    <% } else { %>
                        <span class="badge badge-secondary">Standard User</span>
                    <% } %>
                </div>
            </div>
        </div>
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    User Profile Details
                </div>
                <div class="card-body">
                    <form>
                        <div class="form-group row">
                            <label class="col-sm-2 col-form-label">User ID</label>
                            <div class="col-sm-10">
                                <input type="text" readonly class="form-control-plaintext" value="<%= profileUser.id %>">
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-sm-2 col-form-label">Email</label>
                            <div class="col-sm-10">
                                <input type="text" readonly class="form-control-plaintext" value="<%= profileUser.email %>">
                            </div>
                        </div>
                        
                        <!-- IDOR HINT -->
                        <% if (currentUser.id != profileUser.id) { %>
                            <div class="alert alert-warning mt-3">
                                <strong>Warning:</strong> You are viewing a profile that does not belong to you.
                                <!-- FLAG{IDOR_profile_viewer_success} -->
                            </div>
                        <% } %>
                    </form>
                </div>
            </div>

            <div class="card mt-4 border-danger">
                <div class="card-header bg-danger text-white">
                    API Testing Console (Debug)
                </div>
                <div class="card-body">
                    <p>Developers left this here to test the API easily.</p>
                    <form action="/api/users/<%= profileUser.id %>" method="POST">
                        <div class="form-group">
                            <label>Update User JSON Payload:</label>
                            <textarea name="payload" class="form-control" rows="3">{"isAdmin": 1}</textarea>
                            <small class="form-text text-muted">Warning: This endpoint relies on Mass Assignment.</small>
                        </div>
                        <!-- Simple hack to make the form send JSON-like keys for the node backend to parse via bodyParser.json() logic 
                             In reality, bodyParser.urlencoded won't parse nested JSON string nicely without work, 
                             so we will assume the attacker uses Postman/Curl or we rely on the specific body parser setup.
                             For this lab, let's keep it simple: The attacker should use Curl/Burp. 
                             This form is just a visual hint. -->
                        <button type="button" class="btn btn-outline-danger" disabled>Use cURL or Burp to exploit this!</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>